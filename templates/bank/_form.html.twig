
        <div class="card-header">
            <h3 class="card-title">
                Nouvelle Banque
            </h3>
        </div>
        <!--begin::Form-->
        {{  form_start(form) }}
        <div class="card-body">
            <div class="form-group row">
                {{ form_label(form.name,'Nom de la banque', {'label_attr':{
                    'class':'col-lg-3 col-form-label text-lg-right'
                }}) }}
                <div class="col-lg-6">
                    {{ form_widget(form.name, {'attr':{
                        'class':'form-control'
                    }}) }}
                </div>
        </div>
        <div class="card-footer">
            <div class="row">
                <div class="col-2">
                </div>
                <div class="col-lg-6">
                    <button class="btn btn-success mr-2">{{ button_label|default('Enregistrer') }}</button>
                    <a class="btn btn-success mr-2" href="{{ path('bank_index') }}">back to list</a>
                </div>
            </div>
        </div>
        {{ form_end(form) }}

    </div>

